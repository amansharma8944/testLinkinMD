export type WeatherResult = {
  location: string;
  temp: number;
  condition: string;
};

export type F1Result = {
  raceName: string;
  circuit: string;
  date: string;
};

export type StockResult = {
  symbol: string;
  price: string;
};
